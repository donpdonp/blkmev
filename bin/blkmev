#!/usr/bin/env perl6

use v6;
use lib './lib';
use BlkMeV;
use BlkMeV::Net;
use BlkMeV::Chain;

sub MAIN ( Str $chain_name =  "bitcoin" ) {

  my $chain = BlkMeV::Chain::Chain.new(name => $chain_name);
  my $supplier = Supplier.new;
  my $supply = $supplier.Supply;
  my $payload_tube = Channel.new;
  my $socket;

  say "connecting {$chain.host}:{$chain.port}";

  IO::Socket::Async.connect($chain.host, $chain.port).then( -> $promise {
    CATCH { default { say .^name, ': ', .Str } };
    $socket = $promise.result;
    $supplier.emit('connect');
    BlkMeV::Net::read_loop($socket, $supplier, $payload_tube);
  });

  $supply.tap( -> $inmsg {
    BlkMeV::Net::dispatch($inmsg, $chain, $socket, $payload_tube)
  });

  Channel.new.receive; # wait forever
}

=begin pod
=head1 USAGE
$ blkmev
=end pod

